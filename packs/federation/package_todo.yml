# This file contains a list of dependencies that are not part of the long term plan for the
# 'packs/federation' package.
# We should generally work to reduce this list over time.
#
# You can regenerate this file using the following command:
#
# bin/packwerk update-todo
---
packs/admin:
  "::StatusPolicy":
    violations:
    - dependency
    files:
    - packs/federation/app/lib/activitypub/activity/quote_request.rb
packs/api:
  "::Api::BaseController":
    violations:
    - dependency
    files:
    - packs/federation/app/controllers/activitypub/base_controller.rb
